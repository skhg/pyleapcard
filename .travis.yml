language: python
python:
  - "2.7"
  - "3.6"
  - "nightly"
install:
  - pip install -r requirements.txt
  - pip install .
  - pip install codecov
script:
  - coverage run ./tests/tests.py
  - codecov
